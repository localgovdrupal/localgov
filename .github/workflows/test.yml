name: Test localgovdrupal/localgov drupal-profile

on:
  push:
    branches:
      - '3.x'
  pull_request:
    branches:
      - '3.x'
  workflow_dispatch:

jobs:
  tests:
    uses: localgovdrupal/localgov_shared_workflows/.github/workflows/test-module.yml@fix/1.x/do-not-run-phpstan-if-no-php-files
    with:
      project: 'localgovdrupal/localgov'
      project_path: 'web/profiles/contrib/localgov'
